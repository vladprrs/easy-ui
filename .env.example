# Dokploy API key (Dokploy UI -> Settings -> API/CLI) — used by .claude/skills/deploy/driver.mjs
DOKPLOY_API_KEY=
# Basic-auth credentials of the deployed easy-ui instance, "user:password" (optional, for authed verify checks)
EASY_UI_BASIC_AUTH=
