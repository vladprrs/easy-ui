# План: acceptance pipeline — фидбэк 2026-08-04

**Статус:** v2 после раунда 1 Stage 2 (адверсариальное ревью: 3 линзы + verify, 31 подтверждённая находка / 4 опровергнутых). Источник: `docs/EASYUI_ACCEPTANCE_PIPELINE_FEEDBACK_20260804.md`. Триаж — в конце файла.

## Контекст

`docs/EASYUI_ACCEPTANCE_PIPELINE_FEEDBACK_20260804.md` — фидбэк после первого боевого end-to-end переноса `pay-card-button` (12 cases) и `pay-payment-card` (49 cases, 2 shards) через контур case-set → accept → evidence → promote (план 2026-08-03, в проде, миграции v28, `EASYUI_ACCEPTANCE_MATRIX=1`). 8 проблем: 4×P0 (promote-линковка в CLI, promote отвергает pixel-strict, reuse вердиктов через границу политики, `--refresh failed` проигрывает impact), 4×P1 (ложный not-found, семантика padded reference, лимиты case-set/sparse families + multi-shard provenance, объём JSON), 1×P2 (уровни reuse).

### Корневые причины (верифицированы по коду, уточнены ревью)

- **P0-3/P0-4 — один узел, два дефекта** (`server/acceptance/orchestrator.ts`): порог per-case **входит** в `case_fingerprint` (через `casePolicyHash`), но (a) `forceOf("failed")` (:456-470) ищет провальный вердикт по **новому** fingerprint → после смены порога кэш пуст → форс молча снимается; (b) далее `carryBaselineCase` (`runner.ts:461-505`) переносит вердикт baseline-рана под новый fingerprint **не сравнивая** `casePolicyHash`. Плюс: `fingerprintOf` (`runner.ts:74-85`) не пробрасывает readiness-политику профиля (strict-ран получает `DEFAULT_READINESS_POLICY_HASH`), examples-путь использует заглушку `CASE_POLICY_HASH_V0`. **Уточнение ревью (C8):** дыра шире — `casePolicyHashOf` (`caseSets.ts:57-66`) хэширует профиль **из манифеста**, а не `policyId` рана, поэтому `--policy` не инвалидирует reuse и на case-set-пути; `expectedGeometry` и `cropLineage` случая не входят ни в `casePolicyHash`, ни в `caseFingerprint` — их смена даёт полный stale reuse.
- **P0-2 — эмерджентный отказ, promotion policy не существует**: кандидат при создании всегда штампуется `policyProfileHash(default-v1)` (`server/routes/acceptance.ts:235,245`), а `resolveAcceptanceRefs` (`server/components/promote.ts:126-128`) требует равенства хэша рана и кандидата → любой `pixel-strict-v1` ран падает `acceptance_run_mismatch`. RFC-инвариант: policy вне идентичности кандидата (не пересматривать).
- **P0-1 — чисто клиентский разрыв**: сервер уже принимает `candidateId`/`acceptanceRunId` (роут `components.ts:352-379`, тесты `component-promote.test.ts:310-393`), но `runPromote` в каноне `.claude/skills/author/driver.mjs:1993-2038` их не шлёт и флагов нет. Доп. пробел: одиночный `version()` DTO (`server/repos/components.ts:114`) не возвращает `candidateId`/`acceptanceRunId` (список versions — возвращает). Внимание (C4): `candidate.acceptanceRunId` — это **последний поставленный** ран (`repo.ts:275`), не «принятый»; источником автовыбора быть не может.
- **P1-5 — атрибуция фидбэка не совпадает с кодом**: `GET /components/:id` вообще не кэшируется (`cache.mjs classify:90-106`), «cached component list» не существует. Истинная причина неизвестна → волна начинается с расследования.
- **P1-6**: paint-канва = layout + 2×64px (`DEFAULT_PAINT_MARGIN_PX`, `service.ts:120`) × dsf; двухчастный контракт (padded reference vs `expectedGeometry`=layout root) существует, но неявен; `cropLineage.rect` не валидируется против размеров ассета — ловушка двойного crop.
- **P1-7**: `CASE_SET_MAX_DIMENSION_VALUES=32` (`src/acceptance/caseSetSchema.ts:45`) при `acceptanceMaxCasesPerRun=64` — единственная причина шардирования 49 cases; лимиты не видны в capabilities; валидация только через мутирующий PUT. **Уточнение ревью (C5/C16):** `coverageOf` (`caseSets.ts:294-296`) материализует полное декартово произведение без потолка — подъём лимита без потолка произведения опасен.
- **P1-9**: 1800 строк — `runView` (`server/routes/acceptance.ts:168-207`) инлайнит полные `gates[].metrics` всех failed cases; CLI спредит run в `--json` целиком.

## Ключевые решения

- **D-A (P0-2):** убрать hash-равенство; `acceptance_run_mismatch` = только «ран чужого кандидата»; новый предикат `run.policy_profile_id ∈ PROMOTION_POLICY_PROFILES` (оба встроенных профиля) → иначе новый `422 acceptance_policy_mismatch {runPolicyProfileId, allowed}`. Штамп кандидата не менять (информационный, задокументировать). `capabilities.acceptance.promotionPolicyProfiles` — по AC фидбэка, хотя сегодня предикат тавтологичен (C3: реестр содержит ровно эти два профиля, `startRun` отвергает чужие) — это задел под per-DS конфигурацию, тест ветки отказа — через инъекцию профиля. **Дополнение (C18):** promote сравнивает `run.policy_profile_hash` с текущим `policyProfileHash(ACCEPTANCE_POLICIES[id])`; расхождение (профиль ужесточался после рана) — не отказ, а warning в ответе + оба хэша в provenance опубликованной версии. Strict⇒weak получается автоматически.
- **D-B (P0-3/P0-4), переработано после ревью (C0/C12, C1, C2, C26):** fingerprint расслаивается на **три** слоя, а не два:
  - `frameFingerprint` — входы съёмки: candidateId, caseKey, propsHash, surface, readinessPolicyHash, rendererFingerprint;
  - `comparisonFingerprint` — входы **сравнения** (меняют метрики без пересъёмки): referenceAssetId, referenceSurface, referencePlacement, cropLineage (вкл. sourceSurface), maxDimensionDeltaPx, параметры нормализации канвы (margin, dsf);
  - `verdictPolicyHash` — входы **вердикта** (меняют решение без новых метрик): профиль/пороги (maxRawDiffPct, aa-бюджеты, severity), perCase-оверрайды, requireVisual, состав гейтов, geometry-допуски, expectedGeometry, allowPaintOverflow, expectedClip.
  - `caseFingerprint = hash({algo, frame, comparison, verdictPolicy})`; `CASE_FINGERPRINT_ALGO_VERSION` 5→6 (инвалидация прод-кэша — приемлема); examples-путь хэширует реальный профиль рана.
  - **Четыре пути reuse** (по месту промаха): caseFingerprint совпал → полный reuse; совпали frame+comparison, разошёлся verdictPolicy → **recompute** (чистый пересчёт по сохранённым `metrics_json`); совпал frame, разошёлся comparison → **re-diff** (перезапуск visual-гейта над сохранённым `paint.png` из CAS против нового эталона — без chromium); разошёлся frame → **recapture**. Правило перекрытия гейтов — по **дельте политики**, не по имени гейта (C26): отображение «поле effectivePolicy → затронутые гейты»; гейты вне дельты переносятся законно; затронутый непересчитываемый гейт (readiness, determinism и т.п.) → recapture, никогда не тихий перенос.
  - **Артефакты (C2):** recompute/re-diff перезаписывают производные JSON-артефакты (`visual.json`/`geometry.json`) новыми записями CAS с `derivedFrom: <sha старого>`; байтовые (paint.png, diff.png при recompute) переиспользуются. Манифест evidence и содержимое артефактов пересчитанного случая обязаны быть согласованы (тест).
  - **Алгебра refresh (C1):** `effectiveRefresh = requestedRefresh ∪ impactRefresh`, считается на старте, персистится, печатается тройкой. Refresh получает **гранулярность** `frame | verdict`: `--refresh failed` = verdict-scope (форс инвалидации вердикта; кадр переиспользуется при совпавшем frameFingerprint — это делает достижимым «recapture=0»); `--refresh <ids>` и `--refresh all` = frame-scope (полная пересъёмка, текущая семантика); `--recapture` поднимает failed до frame-scope. `executeCase` принимает скоуп, а не булев `refresh`, и при verdict-скоупе не выключает frame-reuse.
  - `forceOf("failed")` сначала смотрит вердикты baseline-рана (`repo.cases(baselineRunId)`), затем `caseResultForFrame(frameFingerprint, componentId)` (C19 — не по caseFingerprint, иначе смена порога снова опустошает форс; C29 — фильтр по component_id, `ORDER BY last_used_at DESC LIMIT 1`).
  - `422 refresh_scope_empty` — предикат по **факту reuse**, не по размеру скоупа (C10): отказ только если при непустом explicit-скоупе хотя бы один случай отдан из кэша/carry и ни один не был переснят/пере-диффнут/пересчитан; первый ран с пустым кэшем проходит.
- **D-C (P1-7):** поднять `CASE_SET_MAX_DIMENSION_VALUES` до 64 (тест-инвариант «≥ acceptanceMaxCasesPerRun»), все лимиты в capabilities, dry-run `POST /api/components/:id/case-sets/validate` + CLI `case-set validate` (локальный zod-parse + лимиты из кэшированных capabilities **до** сети, сервер — для db-зависимых проверок; C20). **Потолок произведения (C5/C16):** `expectedTuples ≤ 4096` проверяется перемножением длин **до** материализации → `422 case_set_coverage_too_large`, лимит в capabilities; `missingTuples` в ответе усечён. `caseSetGroup` **не строить**: с лимитом 64 семьи ≤64 cases однорановые; хвост >64 закрывает D-D.
- **D-D (P1-8):** promote принимает `acceptanceRunIds: string[]` (1..8, взаимоисключимо с одиночным); проверки: один кандидат, terminal pass, promotion-профиль, единый policy_profile_id, попарная дизъюнктность покрытия по **(caseSetId, caseId)** и по propsHash/caseKey (`422 acceptance_coverage_overlap`; caseId уникален только внутри манифеста — C7), опц. `expectedCases` (`422 acceptance_coverage_incomplete`). **Единый рендерер (C7):** v30 добавляет `acceptance_runs.renderer_fingerprint` (nullable, пишется новыми ранами); promote требует равенства у всех ранов набора, для legacy-строк с NULL проверка пропускается с warning. **Детерминизм (C7):** `acceptanceRunIds` сортируются при записи (`created_at, run_id`), legacy `acceptance_run_id` = первый элемент отсортированного списка — контракт задокументирован. Хранение: плоская TEXT-колонка `acceptance_run_ids` (JSON-массив, без FK — инвариант A9). **GC (C27):** `runIdsReferencedByPublishes()`/`isRunReferencedByPublish()` расширяются union'ом по `json_each(acceptance_run_ids)`.
- **D-E (P1-9), скорректировано (C11/C17/C24):** summary — серверный `?view=summary` с маркером `view:"summary"` в теле; CLI `--summary` строго **opt-in**, смысл `--json` **не меняется** (никакого условного переключения по вердикту); экономию контекста дают скиллы, переведённые на `--summary` в той же волне; link/receipt-store всегда строится из полного рана. Drill-down `accept-status --case <id>`.
- **Гейтинг возможностей (C23):** каждая новая клиент-видимая ручка получает флаг `capabilities.features.*`: `acceptanceSummaryView`, `caseSetValidate`, `acceptanceMultiRunPromote`; driver.mjs проверяет флаг до вызова (конвенция `runProvenance`/`runPromote`); для `?view=summary` клиент дополнительно проверяет маркер `view` в ответе (старый сервер молча игнорирует query).

## Волны

Граф (перестроен после C4/C13/C14 — прежняя «файловая дизъюнктность W1∥W2∥W3» была ложной: пересечения по `routes/acceptance.ts`, `contracts.ts`, `driver.mjs`):

```
W1 ∥ W2a → W3 → W2b → W4
W1 → W5 → W6 → W7 (также после W3) → W8 → W9
```

Сериализация по владению: `routes/acceptance.ts`, `contracts.ts` — W1, затем W3, затем W8; `driver.mjs` — W2a → W2b → W4 → W6 → W7 → W8 (строго по очереди); `docs/server-api.md`, SKILL.md — дописываются каждой волной последовательно (конфликтов нет при сериализации). Миграции: v29 (W1), v30 (W7). Kill-switches: `EASYUI_ACCEPTANCE_VERDICT_RECOMPUTE` (W1, default on; off = всегда recapture при любом промахе caseFingerprint, никогда stale-carry), `EASYUI_PROMOTE_POLICY_STRICT` (W3, default off; on = старое hash-равенство, т.е. возврат P0-2 — только аварийный откат). **Обе переменные объявляются в `docker-compose.yml`** (C21: не объявленная в compose переменная до контейнера не доедет); расслоение fingerprint флагом не откатывается — только откатом образа, см. W9.4.

### W1 — трёхслойный fingerprint, recompute/re-diff, алгебра refresh (P0-3+P0-4) · большая
**Файлы:** `server/acceptance/{ids,caseSets,runner,orchestrator,repo,impact}.ts`, новый `server/acceptance/recompute.ts`, `server/acceptance/gates/visual.ts` (re-diff вход), `server/routes/acceptance.ts` (только run-view), `server/migrations.ts` (v29), `server/contracts.ts`, `docker-compose.yml` (env), `docs/server-api.md`; тесты: новые `recompute.test.ts`, `ids.test.ts` + существующие `orchestrator.test.ts`, `runner.test.ts`, **`server/capture/renderer.test.ts:113` и `server/acceptance/caseSets.test.ts:308`** (пинуют ALGO=5 литералом — C15; инвариант renderer-contract-2 «ровно один bump» пересматривается: комментарий `ids.ts:36-44` переписать, отметку сделать в `docs/plans/2026-08-03-renderer-contract-2.md`).
- `ids.ts`: `frameFingerprint()`, `comparisonFingerprintOf()`, `verdictPolicyHashOf(profile, manifest, caseId)`, пересборка `caseFingerprint`, ALGO=6; examples-путь хэширует реальный профиль рана (конец `CASE_POLICY_HASH_V0` в fingerprint-пути).
- `runner.ts`: `fingerprintOf` получает readiness/профиль из `deps.policy`; `CaseExecution` несёт все четыре отпечатка; каскад reuse: caseFingerprint → (frame+comparison ⇒ recompute) → (frame ⇒ re-diff) → recapture; `carryBaselineCase` сравнивает слои и применяет тот же каскад (`frameReused:true, verdictRecomputed:true, reuseReason:"recompute:policy" | "rediff:comparison"`); дельта-карта «поле политики → гейты», перенос гейтов вне дельты.
- `recompute.ts`: чистый `reevaluateGates(gates, oldPolicy, newPolicy, item) → {gates, reevaluable, changed}` (получает обе политики — дельта вычислима, C26); visual: `rawDiffPct` vs новый бюджет + severityClass; geometry: сохранённые дельты vs допуски/expectedGeometry; остальные — `reevaluable:false`. Перезапись производных артефактов с `derivedFrom` (C2).
- re-diff: вызов visual-гейта с сохранённым `paint.png` (CAS, по `paintSha`) вместо съёмки; новые метрики/артефакты, новый вердикт.
- `orchestrator.ts`: `startRun` считает и персистит `{requested, impact, effective}` со скоупами (`refresh_json`); `forceOf("failed")` — baseline-вердикты first, затем `caseResultForFrame(frame, componentId)`; verdict-scope не выключает frame-reuse в `executeCase`; `carryable` минус `effectiveRefresh`; `refresh_scope_empty` по факту reuse (C10).
- `repo.ts`: `caseResultForFrame(frame, componentId)` (`ORDER BY last_used_at DESC LIMIT 1`, C29), запись `frame_fingerprint`/`comparison_fingerprint`/`verdict_policy_hash` в результат.
- **v29** (всё nullable, без backfill): `acceptance_case_results` +`frame_fingerprint`,`comparison_fingerprint`,`verdict_policy_hash`+индекс по (component_id, frame_fingerprint); `acceptance_cases` +те же+`reuse_receipt_json`; `acceptance_runs` +`refresh_json`.
- API: `run.refresh{requested,impact,effective}` (со скоупами), `progress.frameReused/verdictRecomputed/rediffed`.
- **Тесты:** `recompute.test.ts` (флип вердикта от порога в обе стороны; нереэвалюируемый гейт в дельте ⇒ recapture; гейт вне дельты переносится); `ids.test.ts` (порог меняет case- но не frame/comparison; смена referenceAssetId меняет comparison, но не frame; `--policy` меняет hash на examples- и case-set-путях (C8); ALGO=6 литералом); `orchestrator.test.ts` — **точный репро фидбэка**: смена только порогов + `--refresh failed --baseline-run` ⇒ frames reused=25, verdictRecomputed=8, recapture=0, pass; **анти-репро C0**: смена только `referenceAssetId` при неизменном кандидате ⇒ re-diff (новый rawDiffPct, не reused), никогда recompute; смена только `expectedGeometry` / только `cropLineage.rect` ⇒ другой caseFingerprint и новый вердикт (C8); `runner.test.ts` (отказ carry поперёк verdict-политики; carry гейтов вне дельты); «первый ран с `--refresh failed` и пустым кэшем проходит» (C10); «смена порога + `--refresh failed` **без** baseline-run ⇒ непустой effectiveRefresh» (C19); согласованность манифеста evidence и пересчитанного `visual.json` (C2).
- **Done:** оба P0-репро и анти-репро C0 — тестами; verify зелёный (включая обновлённые renderer/caseSets-тесты); слои, каскад reuse и алгебра refresh задокументированы в server-api.md.

### W2a — promote-линковка в CLI, явные флаги (P0-1, часть) · малая · ∥ W1
**Файлы:** `.claude/skills/author/driver.mjs`, оба `SKILL.md`, зеркала через `scripts/sync-share-skills.mjs`.
- Флаги `--candidate <id>`, `--acceptance-run <runId>` (повторяемый — задел под W7) → `candidateId`/`acceptanceRunId` в теле.
- Локальный pre-flight до POST (rev/sourceHash кандидата vs receipt, run vs candidate); печать выбранной связки до мутации; `--json` и человеческий вывод несут оба id.
- Доки: `.claude/skills/author/SKILL.md:489` («body only») исправить; `share/yp-figma-rebuild-skill/SKILL.md:193,310,326` выровнять с реальными флагами.
- **Done:** `promote --candidate --acceptance-run` даёт версию с обоими id; help/скиллы соответствуют реальности; `driver-cli.test.ts` — флаги в теле, mismatch → локальная ошибка без POST.

### W3 — promotion policy + candidate-view runs[] (P0-2) · средняя · после W1
**Файлы:** `server/components/promote.ts`, `server/routes/{components,meta,acceptance}.ts`, `server/acceptance/policies.ts`, `server/repos/components.ts` (version-DTO), `server/contracts.ts`, `docker-compose.yml` (env), `.claude/skills/author/cache.mjs` (классификация candidate-view), `docs/server-api.md`.
- `PROMOTION_POLICY_PROFILES = ["default-v1","pixel-strict-v1"]`; `resolveAcceptanceRefs` — убрать hash-равенство, профильный предикат → `acceptance_policy_mismatch`; сузить и переформулировать `acceptance_run_mismatch`; warning + двойной хэш в provenance при расхождении hash профиля с текущим определением (C18).
- Capabilities: `acceptance.promotionPolicyProfiles` (+`policyProfiles`).
- Candidate-view: `runs[{runId,status,policyProfileId,caseSetId,finishedAt,promotionEligible}]` — «promotionEligible заранее» из фидбэка (вариант 4) и источник автовыбора W2b. **Кэш (C22):** `component-candidates/:id` в `cache.mjs` переводится из `immutable` в `fresh` (короткий TTL) — там уже сейчас мутабельные `status`/`acceptanceRunId`.
- `version()` DTO (`repos/components.ts:114`): добавить `candidateId`/`acceptanceRunId`. Критерий «DTO несёт оба id» проверяется по 201-ответу promote и списку versions (noStore); одиночный version-DTO отдаётся immutable — ограничение задокументировать (C30).
- Комментарий v25 + server-api.md: `component_candidates.policy_profile_hash` — информационный.
- **Тесты:** strict-ран промоутится; чужой кандидат → `acceptance_run_mismatch`; несовместимый профиль (инъекция) → `acceptance_policy_mismatch`; stale-hash → warning + provenance; capabilities; version-DTO несёт id; candidate-view отдаёт runs[].
- **Done:** репро `pay-card-button` (strict `acc_7462…`) промоутился бы; коды ошибок разведены; runs[] доступен для W2b.

### W2b — автовыбор связки promote (P0-1, остаток) · малая · после W3
**Файлы:** `driver.mjs`, `cache.mjs`, оба SKILL.md, зеркала.
- Автовыбор без флагов (при `acceptanceMatrix`): **прямой сетевой** запрос candidate-view (не из кэша — C22), единственный terminal pass ран кандидата, совпадающий с `{baseRev, sourceHash}` validate-receipt; link-store — только подсказка, не источник истины (C13); 0 → promote без линка с warning (текущее поведение); ≥2 → `CliError` со списком ранов. `candidate.acceptanceRunId` (скалярный) источником не является — это «последний ран» (C4).
- **Done:** автовыбор с тёплым кэшем находит свежий ран (тест); ambiguity → локальная ошибка без POST.

### W4 — existence-provenance (P1-5) · малая-средняя · после W2b
**Шаг 0 — расследование** (атрибуция фидбэка опровергнута кодом): репро с warm `--cache-dir` и свежим draft; кандидаты — 5-мин fresh-кэш catalog-путей в accept-цепочке, identity-mismatch сессии (draft чужого owner'а → легитимный 404), серверная видимость draft с `versions:[]`. Итог зафиксировать; при причинах (b)/(c) фикс корректируется, provenance-поле остаётся.
**Файлы:** `driver.mjs`, `cache.mjs`, оба SKILL.md, зеркала.
- `getMeta` → `{value, provenance: list-cache|direct-cache|direct-network}`; отрицательный не-network результат → ровно один принудительный direct refresh до «not found»; мутационные пути требуют direct-*; `existence{source,refreshed,status}` в `--json`.
- **Done:** свежий draft принимается без `--cache-refresh`; provenance в JSON; тест «настоящий 404 = ровно один refresh».

### W5 — content-hug reference (P1-6) · большая · после W1
**Файлы:** `src/acceptance/caseSetSchema.ts`, `server/acceptance/{caseSets,evidence}.ts`, `server/acceptance/gates/visual.ts`, `scripts/visual-diff-worker.mjs` (+тип опций `diff-runner.ts`), `docs/server-api.md`.
- Схема (аддитивно): `referenceSurface: "content-hug"|"paint"`, `referencePlacement {x,y}?`, `cropLineage.sourceSurface: "figma-node"|"content-hug"|"paint"`. **Все новые поля строго `.optional()` без `.default()`** — дефолт (`?? "paint"`) применяется в потребителе, никогда в схеме: `caseSetIdOf` хэширует `parsed.data`, zod-`.default()` сменил бы контентный адрес всех манифестов и сломал идемпотентность PUT (C6/C25). `expectedGeometry` семантически = layout root (переименовать в доках).
- Все новые поля — входы `comparisonFingerprint` (W1), их смена ⇒ re-diff, не recompute (C0/C12).
- Валидация: `rect` за пределами размеров ассета → `422 crop_rect_out_of_bounds`; `cropLineage`+`content-hug` без `sourceSurface:"figma-node"` → `422 crop_lineage_conflict`; warning на «expectedGeometry похож на padded-канву».
- `visual.ts`: для content-hug сервер сам паддит эталон до канонической paint-канвы (`expectedGeometry|layoutBounds + 2×margin×dsf`, placement default = margin×dsf); crop применяется максимум один раз по `sourceSurface`; `metrics.referenceNormalization{…}`; worker получает явный `padTo` вместо вывода.
- Evidence: immutable source ref (id+sha) + нормализованный дериват с lineage.
- **Тесты:** ловушка `pay-card-button` (136×32 vs 264×160 vs двойной crop 116×12); «повторный PUT исторического манифеста (fixture из artifacts фидбэка) ⇒ прежний `cset_` и `cached:true`» (C25); «content-hug нормализация форсирует re-diff» (C12).
- **Done:** ловушка — прошедшими тестами; идемпотентность PUT и неизменность старых `cset_` id — тестами.

### W6 — лимиты, потолок произведения, capabilities, dry-run validate (P1-7) · средняя · после W5
**Файлы:** `src/acceptance/caseSetSchema.ts`, `server/acceptance/caseSets.ts`, `server/routes/{caseSets,meta}.ts`, `driver.mjs`, `server/contracts.ts`, `docs/server-api.md`, оба SKILL.md.
- Лимит values 32→64; тест-инвариант «≥ acceptanceMaxCasesPerRun» (в server-тесте, схема без server-импортов).
- **Потолок произведения (C5/C16):** `∏|dimensions[i]| ≤ 4096` проверяется перемножением длин **до** `flatMap`-материализации в `coverageOf`/`validateManifest` → `422 case_set_coverage_too_large`; `missingTuples` в ответе ≤64 + `truncated:true`; тест «8×64 отвергается за миллисекунды».
- `capabilities.limits`: `caseSetMaxCases/MaxDimensions/MaxDimensionValues/MaxExpectedTuples/ManifestVersion`; подсказки схемы (обязательность `componentId`, «cropLineage:null запрещён — опускать»).
- `POST /api/components/:id/case-sets/validate` (без записи; отдаёт вычисленный `caseSetId`, cases, coverage, warnings, `wouldBeCached`) + `capabilities.features.caseSetValidate`; CLI `case-set validate`: сначала локальный zod-parse + лимиты из кэшированных capabilities + локальный `caseSetId`, серверная часть — при доступном сервере/по флагу (C20, C23).
- SKILL.md: sparse-семьи = одна каноническая ось.
- **Done:** 49-case семья — один case-set и один ран; все лимиты читаются из capabilities; validate немутирующий (тест); декартова бомба отвергается до материализации.

### W7 — multi-run promote provenance (P1-8) · средняя · после W3, W6
**Файлы:** `server/components/promote.ts`, `server/routes/components.ts`, `server/repos/components.ts`, `server/acceptance/repo.ts` (GC-предикаты), `server/migrations.ts` (v30), `server/contracts.ts`, `driver.mjs`, `docs/server-api.md`, оба SKILL.md.
- Тело: `acceptanceRunIds[]` (1..8) XOR `acceptanceRunId`; проверки: один кандидат, terminal pass, promotion-профиль, единый policy_profile_id, единый `renderer_fingerprint` (legacy NULL — warning), дизъюнктность по (caseSetId, caseId) и по propsHash/caseKey (`acceptance_coverage_overlap`), опц. `expectedCases` (`acceptance_coverage_incomplete`). Гейт: `capabilities.features.acceptanceMultiRunPromote` (C23).
- **v30**: `component_publishes` +`acceptance_run_ids TEXT` (JSON-массив, отсортирован по `created_at, run_id`; без FK — A9); `acceptance_run_id` = первый элемент отсортированного (контракт, C7); `acceptance_runs` +`renderer_fingerprint TEXT` (nullable, пишется новыми ранами).
- GC: `runIdsReferencedByPublishes()`/`isRunReferencedByPublish()` — union по `json_each(acceptance_run_ids)`; тест «promote двумя ранами → свипер не трогает ни один» (C27).
- DTO версий: `acceptanceRunIds`, `evidenceManifestHashes`; CLI: повторяемый `--acceptance-run` / `--acceptance-runs a,b` (порядок аргументов не влияет на хранение — сортировка серверная).
- **Done:** два дизъюнктных shard-рана промоутятся с union-покрытием; пересечение блокируется; одиночный promote байтово совместим по legacy-полю; чтение legacy-строк: `acceptance_run_ids IS NULL` ⇒ `[acceptance_run_id]`.

### W8 — summary + reuse-receipts (P1-9, P2-10) · средняя-большая · после W1, W5, W7
**Файлы:** `server/routes/acceptance.ts`, `server/acceptance/{evidence,orchestrator}.ts` (сборка receipts), `server/routes/meta.ts`, `driver.mjs`, `server/contracts.ts`, `docs/server-api.md`, оба SKILL.md.
- `GET /acceptance-runs/:runId?view=summary` — форма из фидбэка: `{view:"summary", runId, status, progress{…, frameReused, verdictRecomputed, rediffed}, gates, refresh, failedCases[{caseId,gate,raw,aa,cause}], remediationGroups, evidenceUrl}`; `view=full` (default) без изменений; `?case=<id>` фильтр на `/cases`. Гейт: `capabilities.features.acceptanceSummaryView`; клиент проверяет маркер `view` в теле (старый сервер игнорирует query — C23).
- Per-case receipt (`acceptance_cases.reuse_receipt_json` из v29, в EvidenceCaseEntry): `{reuse:{candidate,frame,readiness,geometry,visualMetrics,verdict}, fingerprints:{frame,comparison,verdictPolicy,case}}`; `reuseReason` остаётся производной сводкой; манифест evidence несёт effective policy случая (критерий P0-3).
- CLI: `accept --summary` / `accept-status --summary` / `accept-status --case <id>` — строго opt-in; **смысл `--json` не меняется** (C11/C17/C24); link/receipt-store всегда из полного рана; оба SKILL.md переводят рецепты на `--summary`.
- **Done:** `--summary` на failed-ране 25 cases — <100 строк (size-budget-тест); `--json` байтово стабилен на прежних сценариях (регрессионный тест); `reused` больше не двусмысленен.

### W9 — верификация, доки, репетиция · после всех
1. `npm run verify`; e2e: расширить `e2e/preview/acceptance-run.spec.ts` (threshold-only recompute; reference-change re-diff; content-hug визуальный pass; promote strict-раном end-to-end).
2. `scripts/sync-share-skills.mjs` + `git diff --exit-code share/`.
3. Runtime-репетиция на локальном сервере — репро фидбэка: **R1 в две фазы (C9):** (0) прогрев — полный baseline-ран под ALGO 6; (1) смена порогов+`--refresh failed --baseline-run` ⇒ recapture=0/pass с тройкой refresh; R2 strict-promote; R3 свежий draft с warm-кэшем; R4 49-case одним set/раном; R5 `--summary`; R6 смена эталона ⇒ re-diff.
4. Миграционная репетиция: копия v28-базы → v29/v30 вперёд, старые `cset_` резолвятся, старые publish-строки читаются с `acceptanceRunIds:null`; **обратный тест (C28):** v28-код на v29/v30-БД — старт, чтение ранов/кандидатов/версий, promote старым путём (по образцу `server/visual-renderer-guard.test.ts`).
5. Чек-лист доков: `docs/server-api.md` (W1/W3/W5/W6/W7/W8), `server/contracts.ts`, оба SKILL.md (+ зеркала), `docs/openapi` если генерится из contracts; отметка о пересмотре ALGO-инварианта в `docs/plans/2026-08-03-renderer-contract-2.md`.

## Верификация (итоговая)
- Гейт каждой волны: `npm run verify` зелёный; коммитит оркестратор после независимой проверки done-критериев.
- Финал: verify + полный e2e + runtime-прогон по `.claude/skills/verify/SKILL.md` + репро-сценарии W9.3.
- Деплой — отдельной командой пользователя (по конвенции проекта), с бэкапом перед v29/v30. Первый ран после деплоя — холодный (ALGO 5→6, кэш пуст): ~6 с/case, для 49 cases ≈ 5 мин — ожидаемо, не дефект (C9).

## Триаж ревью (раунд 1, 2026-08-04)

Ревью: 3 линзы (correctness / scope / migration-risk), каждая non-minor находка адверсариально верифицирована. 31 подтверждена, 4 опровергнуты.

**Принято (blocker/major):** C0+C12 (трёхслойный fingerprint + путь re-diff — переписан D-B); C1 (гранулярность refresh frame|verdict); C2 (перезапись производных артефактов при recompute/re-diff); C4+C13 (W2 расщеплена на W2a/W2b, автовыбор после W3, `candidate.acceptanceRunId` — не источник); C5+C16 (потолок декартова произведения до материализации); C7 (сортировка runIds, renderer_fingerprint в v30, дизъюнктность по (caseSetId,caseId)+propsHash); C14 (граф волн перестроен, сериализация по владению файлами); C21 (kill-switches в docker-compose.yml, семантика и дефолты записаны); C22 (candidate-view → fresh-кэш, автовыбор — direct network); C23 (capabilities.features-флаги + маркер view).

**Принято (minor):** C6+C25 (`.optional()` без `.default()`, PUT-идемпотентность тестом); C8 (уточнены корневые причины, тесты expectedGeometry/cropLineage/cross-policy); C9 (R1 в две фазы, цена холодного старта); C10 (refresh_scope_empty по факту reuse); C11+C17+C24 (`--json` стабилен, `--summary` opt-in); C15 (renderer/caseSets-тесты в скоуп W1, пересмотр ALGO-инварианта зафиксирован); C18 (stale-hash профиля → warning + provenance); C19 (`forceOf` fallback по frame-отпечатку); C20 (validate локально-первый); C26 (дельта-карта «политика→гейты» вместо правила по имени гейта); C27 (GC union); C28 (обратный тест отката образа); C29 (сигнатура caseResultForFrame); C30 (ограничение immutable version-DTO задокументировано).

**Принято частично:** C3 (предикат promotion policy сегодня тавтологичен — признано в D-A; поле capabilities и код ошибки сохранены как AC фидбэка и задел под per-DS; per-DS конфигурация — вне скоупа этого плана).

**Отклонено (опровергнуто верификацией):** R0 (recompute геометрии возможен — clipChain редуцируется до clippedBy в метриках); R1 (union-покрытие доказуемо без новой сущности — по манифестам case-set'ов); R2 (GC в проде не вызывается — цена ALGO-bump не растёт); R3 (репро W9.3-R1 идёт через carry-путь baseline-рана, не через fingerprint-кэш; двухфазность R1 всё же принята через C9).
